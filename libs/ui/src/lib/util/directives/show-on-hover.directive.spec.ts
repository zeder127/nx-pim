import { ShowOnHoverDirective } from './show-on-hover.directive';

describe('ShowOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
